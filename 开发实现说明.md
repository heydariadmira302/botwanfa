# BOTWANFA 异步重构第一阶段

## 已实现

- Python 3.12 项目结构，aiogram 3、SQLAlchemy 2 async、asyncpg、Alembic。
- bot、scheduler、worker、sender 四服务，共享一个镜像和 PostgreSQL 状态。
- 群、玩家、独立钱包、钱包账本、群配置、倍率、期次、骰子、下注批次、结算、Outbox 等核心表。
- 每群唯一活动期次；Scheduler 使用 PostgreSQL advisory lock 支持多实例。
- 下注格式：大、小、单、双、dd、ds、xd、xs、和值3至18、顺子、豹子、指定豹子。
- 一条消息多项下注整条校验、整条扣分；群ID和消息ID唯一约束防止重复扣分。
- 三骰全部独立判奖。111 和 666 同时参与大小、单双、组合、和值和豹子。
- 倍率在下注时快照；返还额包含本金。
- Outbox 按群顺序发送；429 按 retry_after 重试；每颗骰子成功后持久化发送进度。
- 发送进程异常中断时转入 manual_review，防止自动重复开奖。
- Argon2id 派生密钥和 AES-256-GCM 流式备份加密，错误密钥或文件损坏会在恢复前终止。
- Docker Compose 以及 Linux、Windows 的安装、更新、启停、状态、备份和恢复脚本。
- 完整中文超级管理员私聊后台、群选择、暂停/恢复、参数和分类赔率设置、玩家查询与上下分；赔率支持分类总览、批量倍率和单项启停。
- 群内固定开盘/封盘视觉模板、封盘投注清单、三颗原生骰子、走势图和全员结算消息流水。
- 开盘和封盘使用项目内置的 `betting-open.gif`、`betting-closed.gif`；普通投注只回复文字，不生成单个玩家投注图。
- Pillow 动态走势图采用最近84期滚动窗口、每行7期并显示完整期号；结果文字和下注玩家提及与走势图使用同一条图片消息，超长提及自动拆分。
- 超长封盘清单和结算表自动生成多页图片，保留全部玩家、ID、投注及余额数据。
- 玩家自行掷骰门槛和超时回退、签到、日周月榜、连胜奖励及管理员审计。

## 启动

Linux：

```bash
bash scripts/linux/install.sh
```

Windows PowerShell：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\windows\install.ps1
```

安装后把机器人加入群，在群内发 `/start` 建立群配置。默认初始余额为 1000.00，之后 Scheduler 自动开盘。

## 验证

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\ruff.exe check src tests migrations
.\.venv\Scripts\alembic.exe upgrade head --sql
```

测试覆盖全部 216 种骰子组合、多项解析整条失败、顺子排列、111/666 叠加命中、状态机和备份密钥校验。

## 数据迁移与回滚

旧版 `dice_bot.py` 和 SQLite 文件未被新服务读取或改写，当前保留作迁移参照。新系统首次部署创建独立 PostgreSQL 数据库。

回滚应用：停止 Compose 服务，重新启动旧版入口即可。回滚数据库：部署前执行备份，应用迁移前使用 `alembic downgrade -1`；正式产生新业务数据后优先从加密快照恢复，不直接降级破坏新数据。

## 后续阶段

- ARQ 定时备份、保留清理和私聊菜单内的完整恢复编排。
- 旧 SQLite 到 PostgreSQL 的一次性导入器与对账报告。
- Docker 环境中的并发事务、429、进程重启、跨群隔离和全新机器部署演练。
